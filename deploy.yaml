- name: api
  become: true
  exec:
    - name: "create a html file"
      command: "touch {name}"
      path: "/var/www/html"
      args:
        - key: "name"
          value: "a.html"
      pass_on_error: false
    - name: "restart nginx"
      command: "systemctl restart nginx"
